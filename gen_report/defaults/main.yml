# Provide the directory where to create the report, defaults to current directory
gen_report_dir: .

# Provide the template to use for generating report
# See files sub-directory for a list of valid templates
gen_report_template: report_fix_levels.template